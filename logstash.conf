input {
  kafka {
    bootstrap_servers => "172.26.0.2:9092"
    topics => ["logs"]
    group_id => "group-1"
    codec => "plain"
  }
}

output {
  elasticsearch {
    hosts => ["http://172.26.0.3:9200"]
    index => "logs-%{+YYYY.MM.dd}"
    timeout => 60
  }
  stdout {
      codec => rubydebug
  }
}
